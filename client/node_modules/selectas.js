import R from 'ramda'

export const getGundamById = (state, id) => R.props(id, state.gundam);

export const getMeGundams = state => {
    // console.log('state.gundamPage.ids: ',state.gundamPage.ids)
    const gundam = R.map(id => getGundamById(state, id), state.gundamPage.ids)
    return gundam
}
