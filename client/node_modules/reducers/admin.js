// import R from 'ramda'


const initialState = {

}

export default (state = initialState ,{ type, payload}) => {
  switch (type){
    // case GET_GUNDAM_SUCCESS:
   
    default:
      return state
  }

}