import R from 'ramda'
import gundamData from "./fakeData";

console.log('getting data..')

export const fetchGundams = async () => {
    // todo Replace with GraphQL call

        // const query = `query {fetchGundams {}}` 

        // const variables = {limit: 100} /

        // const {body} = await request.post('/graphql', {query, variables})
        // return body
        return new Promise(resolve => {
          resolve(gundamData)
          
    })
  }
export const loadMoreGundams = async () => {
    // todo Replace with GraphQL call

        // const query = `query {loadMoreGundams {}}` 

        // const variables = {limit: 100} /

        // const {body} = await request.post('/graphql', {query, variables})
        // return body
        return new Promise(resolve => {
          resolve(gundamData)
          
    })
  }

  export const fetchGundamById = async (id) => {
     // todo Replace with GraphQL call
        return new Promise((resolve, reject )=> {
          const gundam = R.find(R.propEq('id',id),gundamData)
          resolve(gundam)
          
    })
  }

